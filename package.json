{
  "name": "route-planner",
  "version": "1.0.0",
  "description": "Turn an established GPX route into a detailed multi-day itinerary with researched detours, accurate distance and ascent, and per-day GPX exports.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "test:site": "node scripts/test-site.mjs",
    "test:features": "node scripts/test-features.mjs",
    "test:exports": "node scripts/test-exports.mjs",
    "test": "node scripts/verify.mjs && node scripts/test-site.mjs && node scripts/test-features.mjs && node scripts/test-exports.mjs && node scripts/test-server.mjs",
    "package": "node scripts/package.mjs",
    "analyse": "node scripts/analyse.mjs",
    "serve": "node scripts/serve.mjs",
    "lint": "npx --yes oxlint@latest",
    "lint:fix": "npx --yes oxlint@latest --fix",
    "lint:dead": "npx --yes knip@latest --no-progress",
    "format:check": "npx --yes prettier@latest --check .",
    "format": "npx --yes prettier@latest --write .",
    "check": "npm run lint && npm test && npm run analyse",
    "deploy": "APP_MODE=production npm run build && npm test && npx --yes netlify-cli@latest deploy --prod --dir=site",
    "deploy:preview": "APP_MODE=production npm run build && npm test && npx --yes netlify-cli@latest deploy --dir=site",
    "deploy:anon": "APP_MODE=production npm run build && npm test && npx --yes netlify-cli@latest deploy --dir=site --allow-anonymous",
    "preset": "node scripts/preset.mjs",
    "preset:list": "node scripts/preset.mjs list",
    "build:local": "APP_MODE=local node scripts/build.mjs",
    "dev": "APP_MODE=local node scripts/build.mjs && node scripts/serve.mjs",
    "test:server": "node scripts/test-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
